import React from 'react';
import reactDOM from 'react-dom';

const ContactUs: React.FC = () => <>This is the contact Us page</>;
const elem = document.getElementById('app');

reactDOM.render(<ContactUs />, elem);
